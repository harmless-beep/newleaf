<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- New Leaf is fully offline: no INTERNET permission, nothing leaves the device. -->

    <!-- Gentle evening-check-in reminder (opt-in). POST_NOTIFICATIONS is only
         needed on Android 13+, where the user grants it at enable time. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Lets the nudge land on time even in Doze. Denied by default on some
         Android versions; the app gracefully falls back to a near-time window. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|uiMode"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lets Android Studio's profiler attach to release builds too: CPU
             and frame timelines can be inspected with the same tooling as a
             debug build, without shipping debuggable. -->
        <profileable
            android:shell="true"
            tools:targetApi="q" />

        <!-- Shows the gentle reminder when its alarm fires. -->
        <receiver
            android:name=".ReminderReceiver"
            android:exported="false" />

        <!-- Re-schedules the reminder after a reboot so it is never lost. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>