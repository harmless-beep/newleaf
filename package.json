{
  "name": "newleaf",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "New Leaf — a warm, ad-free companion for changing habits gently.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "budget:dirs": "vite build && node scripts/check-bundle-budgets.mjs bundle-stats.json"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0"
  }
}
